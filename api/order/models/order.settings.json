{
  "kind": "collectionType",
  "collectionName": "orders",
  "info": {
    "name": "Order"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "type": {
      "type": "enumeration",
      "enum": [
        "Advertisement",
        "Coin"
      ],
      "default": "Coin"
    },
    "status": {
      "type": "enumeration",
      "enum": [
        "Pending",
        "Completed",
        "Canceled"
      ],
      "default": "Pending"
    },
    "entityId": {
      "type": "string",
      "required": true
    },
    "created": {
      "type": "datetime"
    },
    "updated": {
      "type": "datetime"
    },
    "userNote": {
      "type": "text"
    },
    "user": {
      "via": "orders",
      "plugin": "users-permissions",
      "model": "user"
    }
  }
}
